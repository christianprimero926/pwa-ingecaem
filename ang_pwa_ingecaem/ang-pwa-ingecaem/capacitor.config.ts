import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ang_pwa_ingecaem',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
